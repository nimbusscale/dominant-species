import { GameStateElement } from '../../../app/engine/model/game-element.model';

describe('GameStateElement', () => {
  it('should create an instance', () => {
    expect(new GameStateElement()).toBeTruthy();
  });
});
